{
  "name": "sql-playground",
  "version": "1.0.0",
  "private": true,
  "description": "Modern web-based SQL Query Playground for understanding and optimizing SQL queries",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run sample --prefix backend",
    "test": "npm run test --prefix backend",
    "docker:build": "docker build -t sql-playground:latest .",
    "docker:run": "docker run --rm -p 3001:3001 --name sql-playground sql-playground:latest",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sarth-akvaish/sql-playground.git"
  },
  "author": "Sarthak Vaish",
  "license": "MIT"
}
